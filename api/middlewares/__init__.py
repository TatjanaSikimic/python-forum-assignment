# This should be used for Middlewares, like auth middleware,
# to check if the user token is correct (consider global checks as middleware)

# https://fastapi.tiangolo.com/tutorial/middleware/

# TODO: Add authentication middleware, to check token if token exists (Used primarily for endpoints that need token)
